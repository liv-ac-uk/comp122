public class Student {
    ? boolean[] hasSubmitted = new boolean[4];
    ? int[] grade = ?;

    ? void updateGrade(assignment, grade) {
        // correct index
        
        if {
            System.out.println("Enter an assignment number from 0-3");
            return;
        }

        // correct grade
        if {
            System.out.println("Enter a grade from 0-25");
            return;
        }

        // Update grade
        hasSubmitted[?] = ?;
        ?
    }

    ? getHasSubmitted() {
        return hasSubmitted;
    }

    ? getGrades() {
        ?
    }

    ? getTotalGrade() {
        ?
    }
}
